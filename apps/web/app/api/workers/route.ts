const apiBase = process.env.NEXT_PUBLIC_API_BASE_URL || "http://localhost:8000";
export async function GET(request: Request) { const org = request.headers.get("x-organization-id") || "local-org"; const r = await fetch(`${apiBase}/api/v1/workers`, { headers: { "X-Organization-ID": org }, cache: "no-store" }); return new Response(await r.text(), { status: r.status, headers: { "content-type": "application/json" } }); }
export async function POST(request: Request) { const org = request.headers.get("x-organization-id") || "local-org"; const r = await fetch(`${apiBase}/api/v1/workers`, { method: "POST", headers: { "content-type": "application/json", "X-Organization-ID": org }, body: await request.text() }); return new Response(await r.text(), { status: r.status, headers: { "content-type": "application/json" } }); }
